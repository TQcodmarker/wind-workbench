# 2026-09-11 Excel 清单取数

范围：地方债二级盯盘.xlsx 的「原数据-WIND」A2:A14024，共 14,023 个唯一代码。

当前暂停原因：Wind MCP 返回“余额不足，请先充值”。本轮共发起 96 次数据调用（包含 9 次试取和主批次 87 次调用，最后一次为余额不足拒绝）。这不是金额或已计费次数的估算。

已取得 4,352 券，其中 3,539 券所需字段齐全、813 券仍缺项；另 9,671 券未取得记录。工作台另有原先保存的 4 券不属于本表，未计入上述覆盖数量。

## 余额恢复后继续

在 D:\code\wind工作台 中执行下列命令。当前尚未执行恢复；先恢复 Wind MCP 账户余额。

```powershell
.venv\Scripts\python.exe -m backend.excel_acquisition --universe runtime/excel-universe-20260911.json --target 2026-09-11 --batch-size 100 --max-data-calls 400 --resume runtime/excel-universe-20260911-acquisition-2026-09-11.json --retry-rejected
```

400 是主批次累计调用上限，包含已经发起的 87 次。恢复只允许重发已明确被“余额不足”拒绝、且没有返回数据行的请求；保留原始请求记录和累计次数。已经取得的字段会复用。

可在主任务继续期间启动已验证的单指标补取，以补回部分多指标查询中的空值。每个债券/指标最多补查一次，已到期券不重复查询空行情。该补取尚未启动。

```powershell
.venv\Scripts\python.exe -m scripts.supplement_excel_fields --main-checkpoint runtime/excel-universe-20260911-acquisition-2026-09-11.json --target 2026-09-11 --fields yieldPct duration --batch-size 100 --max-data-calls 200
```

最终统计须同时纳入 runtime/excel-acquisition-inputs-20260911.json 中明确列出的试取会话及实际启动的补取 checkpoint。使用 scripts.record_excel_acquisition 重新核对最终保存数据并更新同一条工作台记录，不能把“名单处理结束”视为“所有字段有值”。

Excel 仅提供债券代码范围，本次行情均以 2026-09-11 查询保存的 Wind MCP 响应为依据。到期收益率采用收盘价口径；提前偿还、发行人赎回未纳入本次查询和筛选。
