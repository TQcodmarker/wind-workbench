# 单券数据完整性测试

> 后续规则已调整：按用户要求直接采用 MCP 非空返回，按查询日期归档，暂不记录或筛选提前偿还及发行人赎回。原有单券数据已通过新规则计算，见 [最新计算结果](D:/code/wind工作台/outputs/26河北23-已有数据计算-20260911.md)。以下保留首次测试及当时的严格校验记录，不代表当前准入结论。

**结论：部分字段已有数值，但尚未通过当前工作台的完整性校验；原 Excel 全部公式所需数据也不完整。**

测试债券：809336.IB（26河北23）。目标日期：2026-09-11。采集时间：2026-09-12。使用工作台已配置的 Wind 债券 MCP，实际调用 4 次数据工具，含握手、工具发现共 7 次协议请求。各数据请求均返回 HTTP 200；未进行自动重试、其他债券查询或行情快照发布。

## 返回数据与判定

| 字段 | 本次返回 | 判定 |
|---|---|---|
| Wind 代码、主证券代码 | 809336.IB | 已取得，可识别同一债券 |
| 跨市场代码 | 236633.SH | 已取得 |
| 简称 | 26河北23 | 已取得 |
| 发行主体、地区 | 河北省人民政府、河北省 | 已取得 |
| 地方债类型 | 板块包含“地方政府一般债” | 可归类为一般债 |
| 发行日期、到期日 | 发行起始日期 2026-04-20；到期日期 2036-04-21 | 已取得；原表专用的“首次发行日期”未单独返回 |
| 原始发行规模 | “发行总额”88.2 亿元，CNY | 有值，但未证明是首次原始规模或是否包含续发行 |
| 票面利率 | 1.89% | 有值；列名带目标日期 |
| 债券余额 | 88.2 亿，币种 CNY | 可表示为 88.2 亿元；列名带目标日期 |
| 收盘净价 | 101.0634 元 | 有值；列名带目标日期 |
| 收盘价到期收益率 | 原字段“2026年9月11日的收盘价收益率”：1.7691% | 有收益率数值，但未明确证明属于到期收益率，暂不作为合格 YTM 输入 |
| 实际收益率/行情日期 | 未返回独立日期值 | 缺失；请求日期和列名日期不能证明实际数据日期 |
| 目标日剩余到期期限 | 9.6082 年 | 第 1 次称“剩余期限_下一行权日”，第 4 次称“实际剩余期限”；两者是否均指最终到期需核对，不能只因数值相同就视为等价 |
| 提前偿还、发行人赎回标志 | 无明确“是/否”；特殊条款及赎回下一行权日为空 | 缺失，不能从空白推断“不含条款” |
| 收盘价修正久期 | 8.7035，列名明确基于净价 | 有值，但单位未返回；属于收盘价口径，不能改称中债久期 |
| 中债估价修正久期 | null | 本次未取得；未返回具体缺失原因 |
| 中债曲线 4202 收益率 | null | 本次未取得；曲线名称、实际日期、期限点及取点说明也未返回 |

“收盘价收益率.债券价格类型=收益率”只描述返回值的价格类型，不能单独证明它是到期收益率。“发行人赎回权下一行权日.选择权类型=发行人赎回权”同样不能证明该券有或没有赎回条款。

本次没有取得明确的“不支持”或“权限不足”说明。空值只能证明本次未取得该字段，不能据此认定 Wind 全产品不提供。

## 当前工作台校验

将四次原始响应在本地交给现有映射器 `wind-fields-v3-ytm-close` 和计算校验器，结果为 `excluded / missing`。主要阻断项为：

1. 收益率类型、来源实际行情日期没有得到确认。
2. 提前偿还和发行人赎回两项明确标志缺失。
3. “发行总额”的首次原始规模口径没有得到确认。
4. 剩余期限存在语义待核对项，且第 4 次返回的“实际剩余期限”尚未纳入现有映射别名。

第四项同时涉及取数字段定义和本地映射适配；不能将映射器输出为空全部解释为接口没返回数值。收益率 1.7691% 已完整保留在原始响应中，校验结果未将其直接作为合格 YTM 入库。

这只券的发行起始日期在 2025-08-08 之后，即使后续数据补齐，也应归入“2025-08-08 当日及以后发行”组，与当前浏览页面的“此前发行”筛选不同。

## 后续处理顺序

先在有相应权限的 Wind 终端/数据接口中核对“收盘价收益率”的指标定义及实际数据日期，并确认剩余期限和条款字段。随后补齐原始发行规模的取数口径，再调整已确认字段的本地映射。原 Excel 公式复刻还需要单独补齐曲线 4202 和中债久期。本次结果不足以证明其他债券、其他日期或全市场字段的覆盖情况。

## 核验凭据

- 会话 ID：`ea76c1f6-40ed-48c3-b77d-df8184368747`。
- [查询问题与原始结果](../runtime/single-bond-ytm-809336-20260911.json)。
- [完整请求响应证据](../runtime/single-bond-ytm-809336-20260911-evidence.json)。
- [本地映射与计算校验结果](../runtime/single-bond-ytm-809336-20260911-validation.json)。
- [本次限定单券及调用次数的测试脚本](../scripts/probe_single_bond_ytm.py)。

鉴权信息仅在本机内存中用于请求认证，没有写入以上报告或响应导出。
