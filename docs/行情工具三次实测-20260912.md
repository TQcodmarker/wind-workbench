# get_bond_market_data 三次实测

本次按用户要求重新调用已配置的官方Wind债券MCP，所有数据请求仅使用 `get_bond_market_data`。样本为809336.IB（26河北23）、809335.IB（26湖南15），主目标日2026-09-10，另以26河北23在原Excel保存日2026-06-17作单指标对照。

会话：`3298d491-325f-4c36-a0ce-17fec9aad579`。

共3次数据查询，另有初始化、初始化通知、工具发现各1次，共6次协议请求。无自动重试，未启动全量更新或发布行情快照。三次数据查询HTTP均为200，均返回表格。金额计费未由响应提供。

## 1. 本次结论

**工具能返回部分行情数据，但本次仍未取得指定样本的中债估值收益率。**

新取得了两券2026-09-10的收盘价、债券余额、剩余天数及收盘价修正久期。它们分别保留来源字段含义，不替代中债净价、全价、中债收益率或中债修正久期。

## 2. 第一次：目标日中债估价

请求ID：`99d5bf84-7951-4321-8ba7-88a9efc318c8`。

问题：查询两只样本在2026-09-10的中债估价收益率、中债净价、全价、应计利息、中债修正久期和来源实际估值日期，保留原始字段、单位及缺失值。

| 返回字段 | 26河北23 | 26湖南15 |
|---|---|---|
| 中债估值收益率（%） | null | null |
| 中债估值净价（元） | null | null |
| 中债估值全价（元） | null | null |
| 中债应计利息（元） | null | null |
| 中债修正久期 | null | null |
| 中债最新估值日期 | null | null |

代码和简称匹配请求，两券币种均为CNY。HTTP成功不等于指标有值，也不能据此认定当日没有中债估值。

## 3. 第二次：目标日其他行情字段

请求ID：`d5c39703-ccdd-4e28-a3c1-dc65a64d689f`。

问题：查询相同两券在2026-09-10的收盘价、收盘价收益率、成交量、成交金额、剩余到期期限（年）、债券余额和修正久期。

| 实际返回字段 | 原始单位 | 26河北23 | 26湖南15 |
|---|---|---:|---:|
| 2026年9月10日收盘价 | 元 | 101.7967 | 101.7941 |
| 2026年9月10日债券余额 | 亿 | 88.2 | 272.27 |
| 2026年9月10日剩余期限 | 天 | 3511 | 2402 |
| 2026年9月10日剩余期限.周期 | — | 日 | 日 |
| 2026年9月10日收盘价修正久期 | 未明示 | 8.7063 | 6.1324 |
| 2026年9月10日收盘价收益率 | % | null | null |
| 2026年9月10日成交量 | 元 | null | null |
| 2026年9月10日成交额 | 元 | null | null |

日期体现在字段名称中，未额外返回独立行情日期列。单位按响应保留，包括成交量被标成“元”；本次无成交量数值可用于验证该单位。收盘价未明确净价/全价类型，不自行认定其为中债估价价格。

请求剩余期限单位为年，实际返回天数；请求修正久期，实际返回的是收盘价修正久期且没有单位字段。若接入工作台，需要确认期限换算规则、久期来源和单位，不能仅按相似列名直接映射。

这是此前数据清单的新增证据：最近两只样本现在也有明确目标日的余额，不再只有旧上海批次有余额；但它不代表最近12券或全国余额均已补齐。

## 4. 第三次：原Excel已保存非空值的同券同日对照

请求ID：`8d7a3c0a-95aa-440e-b79e-5f45c29ff582`。

自然语言问题明确写入原Excel指标名：

> 查询809336.IB（26河北23）在2026-06-17的中债估值收益率，对应Wind Excel指标b_anal_yield_cnbd(代码,日期,1)。仅返回该指标、单位、实际估值日期、证券代码与数据来源，保留原始字段名。无法获取时明确区分空值、未支持字段和查询错误，不以收盘价收益率、其他市场代码或其他来源替代。

这仍是自然语言question参数，并不是直接执行Wind Excel函数。

返回：

- 代码：809336.IB，简称：26河北23。
- 2026年6月17日中债估值收益率（%）：null。
- 实际估值日期：2026-06-17。
- 没有单独返回数据来源或缺失原因字段。

原Excel同券同日保存的中债估值为1.829%，本次MCP未返回收益率。历史缓存不能保证当前接口必须返回同值，但可说明“已经注明原字段且在原表有保存值的样本，本次查询仍未得到收益率”。本次日期字段非空，然而收益率为空，不能认定已取得一条可用估值。

## 5. 与此前响应的差异

此前请求 `9c1e7cdb-ed2f-4e19-8eed-5722ace18d82` 返回两券2026-09-10“收盘价收益率”分别为1.7691%、1.5905%。本次第二次查询的相同名称指标却均为null。

保留两次证据，不用旧值填入新响应，不把本次空值记为0。查询表述和字段组合不同；自然语言解析、覆盖、数据修订、缓存或服务后端都可能需要核实，但本次无法确定差异原因。不能据此宣告该指标永久不支持，也不能认为只要再换说法就一定有值。

## 6. 对原Excel计算的影响

- 新余额可作为对应两券、对应日期的原始输入。
- 收盘价和收盘价修正久期可单独展示为行情指标，不能冒充中债估价价格和中债修正久期。
- 原Excel所需中债估值收益率仍未补齐，K/P综合收益、L/M曲线偏离和地区中债估值汇总仍不能完成。
- 票面利率、曲线4202、明确条款和全国完整名单不在这三次行情测试的验证范围内。

## 7. 留档

- 精简报告与返回表：`runtime/market-fields-test-20260912.json`。
- 完整原始响应及片段：`runtime/market-fields-test-20260912-evidence.json`。
- 工作台来源数据库：`runtime/wind-wind.sqlite3`，按上述会话ID查询。
- 数据溯源页面会话名称：行情工具实测：中债估值与其他字段（最多3次数据查询）。

响应与导出使用已有凭据脱敏机制，不包含认证头。执行入口设置3次数据查询上限；本轮输出存在时拒绝重复查询。
