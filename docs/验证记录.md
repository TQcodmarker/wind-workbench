# Demo 验证记录

2026-09-12 新增大模型配置：最终后端测试为 18 项，浏览器检查为 16 项，构建通过。新增验证覆盖配置持久化、Key 仅保留内存且不回显、更换服务地址不复用 Key、无鉴权本地服务、URL/超时校验、模型测试请求契约、鉴权/超时错误处理、修改后清除旧测试状态、页面刷新恢复与撤销/清除。模型响应测试使用模拟服务响应，未使用真实服务商 Key。页面截图为 `test-results/model-config.png`。

实现目录：`D:\code\wind工作台`。原始参考材料来自 `C:\Users\Admin\Documents\ChatGPT\wind工作台`，原型与原始文档未修改。

## 构建与后端

`npm.cmd run build` 通过，包含 TypeScript 严格类型检查和 Vite 生产构建。

`python -m unittest discover -s tests -v`：13 项全部通过。

- 发行规模加权，整体不取两个平均值的平均；完整 1,554 个位置。
- 2.5、3.5、10.5、29.5 等期限边界，2025-08-08 当天分组。
- 空值、有效零收益率、非数值、错误状态、估值日期、未知条款、非正规模、重复身份及冲突重复。
- 兵团与新疆独立统计。
- SQLite 插入过程中触发真实事务失败，验证整个新快照回滚且旧快照不变。
- 重试失败与无数据响应不撤销成功结果；同日队列去重、跨日保存。
- 1.75% 到 1.82% 为 +7BP，零值可比、空值不可比、样本和规模取自快照。
- 摘要无基准、明确指定缺失基准、自动摘要覆盖两个发行日期组。
- 第三档自然语言筛选覆盖当前页面遗留的单地区条件，返回 18 个地区。
- 本机 Host、写入来源、日期与参数校验，Wind Key 不回显。
- 独立队列执行；摘要生成失败后估值仍成功保存。

## 浏览器

使用 agent-browser 配合本机 Chrome 验证页面加载与控件，再以 Playwright 执行完整交互。浏览器测试使用独立测试数据库与 8766 端口，正式 Demo 历史不受测试修改。

`npm.cmd run test:e2e`：15 项检查通过，包括：完整矩阵、1440 表内滚动、1920 全列、零值详情与键盘焦点归还、URL 恢复、无数据与空组、失败保留旧快照、CSV 下载、历史对比、模板摘要、自然语言查询、凭据配置与不持久化到浏览器、独立任务重试后刷新持久化、390px 窄屏，以及无浏览器脚本/控制台错误。

报告：`test-results/browser-report.json`。

截图：`test-results/desktop-1440.png`、`desktop-1920.png`、`comparison.png`、`source.png`、`mobile.png`。

## 未验证或未启用

- 未使用真实 Wind Key；真实 MCP 握手代码未对授权服务作端到端测试。
- 未接入真实 Wind 历史数据、生产大模型或生产凭据存储。
- 未安装系统 09:00 定时任务。手动每日入口仅执行演示来源。

以上限制在页面与 README 中均有标识。
